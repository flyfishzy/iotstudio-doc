{
  "name": "iotstudio-doc",
  "version": "1.0.0",
  "license": "BSD",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flyfishzy/iotstudio-doc.git"
  },
  "scripts": {
    "build": "gitbook build",
    "prepublish": "npm run build",
    "publish": "git subtree push --prefix=_book origin gh-pages"
  },
  
  "devDependencies": {
    "gitbook-cli":"^2.3.2"
  }
}
